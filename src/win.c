#include "win.h"

int win_current(win_t * win) {
}

void win_parse(const char * fmt, win_t * win) {
}

int win_poll(const win_t * win) {
}

size_t win_format(const win_t * win, char * buf, size_t size) {
}
